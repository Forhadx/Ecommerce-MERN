import React from "react";
import Spinner from "../../components/UI/Spinner/Spinner";

const Home = () => {
    return (
        <div>
            <h1>home</h1>
            <Spinner />
            <h2>spinner</h2>
        </div>
    );
};

export default Home;
